(declare-fun p () Bool)
(declare-fun q () Bool)
(declare-fun s () Bool)
(declare-fun f () Bool)

(assert (=> (not q) s))
(assert (=> (not s) q))
(assert (=> (and q (not s)) p))
(assert (=> (and s (not f)) f))


(check-sat)
(get-value (p q s f))
(check-sat)
(get-value (p q s f))
(check-sat)
(get-value (p q s f))
(check-sat)
(get-value (p q s f))
(check-sat)
(get-value (p q s f))
(check-sat)
(get-value (p q s f))
(check-sat)
(exit)
