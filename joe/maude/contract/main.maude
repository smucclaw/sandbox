load ./verification/model-check  

(rewrite
  start

  < 'clock : Clock | time : (T:Time) >

  < 'lender : Actor | none >
  < 'borrower : Actor | none >

  < 'pid : Process |
    rules : (Rs0:Set{Rule}),
    activeRules : (
      PARTY 'borrower MUST DO 'pay_payment WITHIN (T0:Time),
      PARTY 'borrower MAY DO 'pay_payment WITHIN (T1:Time)
    ) >
.)

eof

(reduce
  modelCheck(none, [] Top)
.)