load model-checker
load ../semantics/semantics

(omod MODEL-CHECK is
  protecting ABSTRACT-MACHINE .
  including MODEL-CHECKER + SATISFACTION + LTL .

  subsort Configuration < State .

  ops Top : -> Prop .

  vars C : Configuration .

---(
  The satisfaction relation has type
    _ |= _ : Configuration -> Prop -> 2
)
  eq C |= Top
  = true .
endom)