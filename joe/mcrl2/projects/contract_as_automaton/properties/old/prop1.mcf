forall m, n : Pos.
forall payment_type : PaymentType.
	val((m == 1075 || m == 525) && n <= initial_outstanding_amt) =>
	[
		true* .
    	pay(accelerated, borrower, lender, m) .
		true* .
		pay(payment_type, borrower, lender, n) .
		true* .
		fulfilled
	]
	false